key <%= scope.lookupvar('::openshift_origin::dns_infrastructure_zone') %> {
  algorithm <%= scope.lookupvar('::openshift_origin::dns_infrastructure_key_algorithm') %>;
  secret "<%= scope.lookupvar('::openshift_origin::dns_infrastructure_key') %>";
};
